# Components

This folder is the approved reusable Manim component library.

Add code here only when it has been extracted from an approved video or explicitly approved for shared reuse. New spikes should use local helpers inside their own video folder until the video is promoted.

Before adding a component, check whether an existing helper can be extended without making it video-specific.
